



export default class PackageJsonConstructor {
    CheckValidInput(packageJson: any): boolean {
    // Add validation logic here if needed
        return true;
    }
    constructPackageJson(packageJson: any): any {
    // You can add or modify properties of packageJson here if needed
        return packageJson;
    }
}